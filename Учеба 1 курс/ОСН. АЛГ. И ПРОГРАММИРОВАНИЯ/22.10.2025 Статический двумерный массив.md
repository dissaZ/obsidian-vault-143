# Статический двумерный массив
[[оаип]]
## Особенности
- память выделяется одним блоком
- двумерный массив - это массив массивов
- имя массива - указатель-константа на первый элемент (первый подмассив)
- для обращения к элементу нужны два индекса
- может инициализироваться полностью или частично
- как правило, используется для хранения матрицы по строкам
- выход за пределы "строки" является переходом к следующей "строке"

- - - 
## ==Во внутреннем цикле условие `j<=i -` это все что на диагонали и ниже!

![[image-52.png|497x221]] 
- - - 
# Симметрия квадратной матрицы
- сравниваем каждый элемент a`[i][j]` с симметричным ему лементом a`[n-1-j] [n-1-i]` 
- проверяем только элементы выше побочной диагонали `(где i+j<n-1)`
- если все пары совпадают > матрица симметрична (flag = 1)
- если найдено несовпадение > матрица несимметрична (flag = 0)
- пример: Для элемента `a[0][0]` симметричный элемент - `a[n-1][n-1]`
- - -
