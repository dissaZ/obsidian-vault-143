
```
#include <stdio.h>
#include <stdlib.h> //лишним никогда библиотека не будет//
#include <windows.h> //важная библиотека для корректного вывода русского языка в консоли

  

int main()

{

    SetConsoleOutputCP(65001);

```


```
#include <windows.h> //важная библиотека для корректного вывода русского языка в консоли

SetConsoleOutputCP(65001); тоже надо для корректной работы русского языка в терминале vscode

```
